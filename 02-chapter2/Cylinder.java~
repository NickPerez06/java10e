/*This program will compute the area and volume of a Cylinder. User will be uask to enter radius and length in that order.
Nicholas Perez
*/

import java.util.Scanner;

public class Cylinder {
	public static void main(String[] args) {

		//Declare pi as constant
		final double PI = 3.14159;

		//creating Scanner object
		Scanner READ_IN = new Scanner(System.in);
	
		double radius, area, volume, length;

	
		//Asking for user input and then reading it in.
		System.out.print("Enter the radius and length of a Cylinder: ");
		radius = READ_IN.nextDouble();
		length = READ_IN.nextDouble();

		//Computing area and volume
		area = radius * radius * PI;
		volume = area * length;

		//Displaying out
		System.out.println("The area is " + area);
		System.out.println("The volume is " + volume);
	}
}
		
